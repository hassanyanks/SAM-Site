extends common_layout

block append content

    div.products-container
        div.product-container.product-wide
            h2 #[strong #{product.name}]
            img(src="/images/" + product.image)
            h2 #[strong $#{product.price}]
